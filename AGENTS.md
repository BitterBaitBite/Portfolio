# AI Agent Guidance for this Repository

## Current state

- This workspace is currently empty and contains no source files, build scripts, or documentation.
- Before making assumptions, verify whether new files have been added and ask the user for the intended project type.

## When helping with this repository

- Ask the user what kind of application they want to build (e.g. personal website, portfolio, blog, React app, static site).
- Ask for preferred tooling if not specified (e.g. Next.js, Vite, Astro, plain HTML/CSS/JS).
- Do not generate large scaffolded code unless the user explicitly requests it.

## If files appear later

- Prefer existing project conventions over generic templates.
- Look for package manifests (`package.json`, `pyproject.toml`, etc.) and README files to infer build/test commands.
- Link to repository documentation when available instead of duplicating it.
